{% extends 'admin_base.html.twig' %}

{% block body %}
    <div class="col-lg-8" style="margin-top: 120px; margin-bottom: 0">
        <div class="card">
            <div class="card-header"> </div>
            <div class="card-body">
                <div class="card-title">
                    <h3 class="text-center title-2">View User</h3>
                </div>
                <hr>
                <p>{{ user.email }}</p>
                <hr>
                <a href="/user" class="btn btn-info">Go Back</a>
                </div>
    </div>
{% endblock %}